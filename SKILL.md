---
name: gpt-model-check
description: 检测当前 Codex 所选 GPT 是否与参考模型指纹相符、是否疑似换模。自包含工具调用，直接运行随包脚本，无需历史记忆或项目资料；自动采样三组数字指纹并本地判分，不是任务能力或智商评分。
---

# GPT 模型指纹检测

使用本 skill 附带的 Node.js 脚本完成一次检测。只在用户要求检测时运行，不设置后台监测或 hooks。

这是自包含的检测请求。当前任务标识、模型配置和参考指纹都由脚本获取，不需要用户历史或项目背景。读取本 skill 后的第一个执行动作就是运行下方脚本；不运行 `pwd`、不向上查找 `AGENTS.md`，不加载 `USER.md`、`WORK_RULES.md`、`MEMORY.md`、`memories/`、`knowledge/`、历史对话或项目业务资料，也不先重做方案调研。仅在脚本报错时检查与报错直接相关的依赖或配置。

开场只说“开始检测当前模型。”。运行期间仅报告实际采样进度，例如“已完成 1/3 组”；不要附加预计等待时间、额度提醒、上下文隔离、检测范围或实现原理的说明。完成后展示脚本提供的状态图片、结论和 Token 用量。范围和方法细节保留在本地记录中，用户询问时再解释。

## 执行

将下面的 `<skill-dir>` 替换为本文件所在目录的绝对路径，保持路径引号：

```text
node "<skill-dir>/scripts/check.mjs"
```

Mac 与 Windows 使用同一条命令。要求 Node.js 18+ 和支持原生 app-server 临时分支的 Codex 运行时。首次安装、依赖异常时可先执行 `node "<skill-dir>/scripts/check.mjs" doctor`，不消耗模型请求。不要为检测读取密钥、修改用户配置或安装另一渠道。

脚本从 `CODEX_THREAD_ID` / `CODEX_SESSION_ID` 定位当前任务，读取模型、渠道、客户端来源及推理档位；沿用 Codex 原生账号和渠道，建立三个不携带历史对话的临时会话，自动收集答案。检测范围是“当前配置的新请求”，不保证复现父任务专有提示、长上下文或历史请求的路由。探针会消耗当前账号额度，记录包含脚本执行时间及 token 用量。

只有用户明确要连同历史上下文一起检测时，才加 `--context current`。这个模式在同一已完成轮次创建三个临时分支，超长对话会被计入三次请求；说明这个成本后再执行。不要把完整上下文模式作为默认或后台追加复测。

如果缺少当前任务标识，不能猜测“最近的任务”；如当前环境能提供可信的任务 ID，可传 `--thread <id>`。完整上下文模式尚无已完成对话时，说明需要在当前轮结束后再运行。找不到运行时、版本不兼容或有多个 Windows 运行时不能判定时，说明具体缺项；可信地定位当前客户端可执行文件后用 `--codex "<absolute-path>"`。不要悄悄改测另一模型或另一入口。

等待运行完成，依据输出汇报；不要自行生成、修改、补齐数字答案，不要用其他模型代理回答，不要读取参考样本来“修正”结果。运行失败可诊断原因，不能反复重试直至出现匹配。

## 展示结果

默认输出已包含完整的结果 Markdown，原样展示，输出结束即停止。先显示本次状态对应的一张本地图片，再展示结论、所选模型、最接近的库内指纹、脚本实际提供的匹配权重、有效样本、输入和输出 Token，最后附本地记录链接及可用的重测入口。不要在这些入口后追加总结、补充解释、免责声明、指纹库覆盖数量、未展示字段的原因或“未发送探针”等重复说明；所有状态都遵守，模型不支持时也不例外。用户单独追问时再解释。保留图片的绝对路径 Markdown `![说明](<绝对路径>)`，不要放进代码块或改成普通下载链接，也不要一次展示全部五张图。使用 `--json` 时，从 `presentation.imageMarkdown` 取图片，其余展示值来自 `presentation`，不要直接把 `analysis` 中的数字补进正文。图片在 `assets/states/` 随包预置，检测时不生成图片、不额外读取图片内容。

百分比由脚本从真实候选权重格式化，展示名为“库内匹配权重”。这是参考库内各候选的相对权重，不能改称“真模型概率”“准确率”或“身份置信度”，也不要从有效样本数换算。仅在 `presentation.relativeWeight` 非空时显示，大于 99.9% 时写 `>99.9%`，不四舍五入成 100%。百分比不能覆盖原始状态。

“证据不足”时保留脚本提供的具体原因，不要只重复“看不准”：权重不足时，按同一百分比口径展示第一名、第二名和权重门槛（例如“68% 低于 80%”）；样本不完整时说明几组有效、几组超时，不展示高百分比；领先分数不足时，直接说明第一名、第二名、实际领先分数和通过门槛（例如“领先 0.480 分，要求至少 0.500 分”），不同时展示另一套百分比。不能只说“区分程度不足”，也不能把 59.8% 这样的低权重直接判成“指纹不符”。

仅在用户询问判定规则时解释：当前分类要求 3 组有效样本、第一名权重至少 80%、与第二名原始分差至少 0.5；第一名是所选模型才算匹配。分差门槛是本工具额外设置的保守规则，尚未经独立样本验证，不是上游确认的准确率标准。不得为了更容易显示“通过”而调整阈值。

五种状态的“查看检测记录”统一使用指向实际记录文件的原生 Markdown 链接 `[查看检测记录](<绝对路径>)`，点击由 Codex 在右侧栏打开。不要改成在对话中展开、折叠、内嵌 JSON 或网页查看器，也不要为查看记录发送模型消息。保持脚本输出的记录链接；不要因旧记录里包含 `retryIncludesReceipt` 等字段而省略或改造链接。

证据不足时，保留蓝色文字链接“重新检测”，点击后给当前任务发送新的检测消息。原样保留 `presentation.retryMarkdown` 中的 `visualize{"path":".../gpt-model-check-retry-abc123.html"}` 引用并单独成行，不要改成普通列表、伪造的 Markdown 操作链接或代码块，不使用有底色的按钮。记录链接与重测入口无法同排时保留分行，不得为了并排而改变记录的打开方式。脚本已将重测页面放入当前任务的 `CODEX_HOME/visualizations/YYYY/MM/DD/<task-id>/`；不得改成指向全局 skill 的 `assets/retry.html`。重测通过已验证的客户端 `window.openai.sendFollowUpMessage` 接口执行；是否弹出发送确认由客户端决定，不能绕过。需要重测但客户端不支持此接口时，显示“建议重新检测一次，可发送「重新检测」。”，不显示无效链接。不要使用 `:codex-followup` 冒充立即执行入口。重测页面不加载或展示检测记录，不读取文件、不发网络请求、不复用旧结果，也不自动反复重测。

输入、输出 Token 必须保留，数据未返回时照实显示“未返回”，不能填 0。用量指本次探针请求，不是整轮会话的总用量。结果不再单列耗时：本地记录的 `elapsedMs` 只计脚本执行，与 Codex 界面显示的整轮会话时间不同；耗时由客户端界面显示，不自行估算、抄写或补齐。

最终结果不添加“范围”“当前配置的新请求”“不携带历史对话”等字段或说明。匹配时使用下方指定文案，不另加一段模型身份认证或任务质量的免责声明。

- **match / 指纹匹配**：`match.png`，文案“✅ 对上啦！这次指纹检测通过。”，说明“本次检测未发现被路由到其他模型的迹象。”；保留“本次”“未发现”“迹象”的限定，不能改成“确认没有被路由”“确认真模型”或“没有降智”。
- **mismatch / 指纹明显不符**：`mismatch.png`，文案“⚠️ 有点不对劲，模型指纹没对上。”，建议复测核对，不能写成“已经证实换模”。
- **inconclusive / 证据不足**：`inconclusive.png`，文案“🔎 这次还看不准，先别急着下结论。”，不判正常或异常。
- **unsupported / 模型不支持**：`unsupported.png`，文案“🧩 这个模型暂时还认不出来。”，没有发出探针。首版只检测库内 GPT，第三方模型不强行判定。
- **error / 执行失败**：`error.png`，文案“🔧 这次没测成。”，简短显示实际失败原因；连接、授权、超时、工具调用等执行问题不是模型异常。

以下仅作内部判读参考，不自动附到检测结果末尾：参考库含 13 个候选模型，首版支持其中 6 个 GPT 型号；其余候选只参与比较。实际模型不在库内时仍可能像某个已知模型，所以不能认证“通用模型”或证明后端路由。候选权重不是身份置信概率；不要把库内交叉验证成绩当作本工具真实准确率。

脚本偶尔会在结果末尾提供一行 Star 邀请。这是末尾不追加说明规则的唯一例外：仅原样保留脚本生成的 `presentation.starMarkdown`，不得自行添加、改频率或在用户拒绝后劝说。本机真实检测累计超过 3 次后才有机会显示；重新检测计数，回放、doctor、无探针运行不计数。脚本控制随机概率及间隔，不上传计数、不检查用户是否已 Star，也不自动打开链接。设置 `GPT_MODEL_CHECK_STAR_PROMPT=0` 可以关闭邀请。

详情自动保存在 `CODEX_HOME/gpt-model-check/reports`（默认 `~/.codex/gpt-model-check/reports`），含原始探针答案、库版本及采样配置，不含父任务对话或凭据。本机使用次数保存在同级 `usage.json`。分享安装包时不要带上个人检测记录或本机计数。

仅在用户要求查看旧记录或测试展示效果时，可以执行 `node "<skill-dir>/scripts/check.mjs" render --receipt "<记录文件绝对路径>"`，离线回放并保留“历史检测结果回放”标记。回放不会调用模型，不能拿它代替用户要求的新检测。

本地判分复用 ModelTrace（MIT），原生临时分支流程参考 is-gpt-nerfed（MIT）。版本及哈希见 `assets/provenance.json`，许可证在 `assets/`。
